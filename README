#############This is a example for Docker and Kubernetes: Create a production-grade workflow

1. First create a docker which has node installed on installed
2. In the docker containter, do `npx create-react-app client` to autogenerate the React app
3. Create a DockerFile.dev in the client folder and then create the docker for it 